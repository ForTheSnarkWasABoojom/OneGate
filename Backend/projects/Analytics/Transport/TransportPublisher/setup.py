from setuptools import setup, find_packages

setup(
    name='TransportPublisher',
    version='0.3.3',
    description='Library that incapsulates messaging with aio_pika',
    packages=find_packages()
)
