from setuptools import setup, find_packages

setup(
    name='ContractModels',
    version='0.3.5',
    description="Models: CreateAnalyticsReport(has inner AnalyticJobDto), AnalyticsReportCreated ",
    packages=find_packages()

)
