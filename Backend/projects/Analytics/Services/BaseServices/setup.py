from setuptools import setup, find_packages

setup(
    name='BaseServices',
    version='0.1.3',
    description='implements Base abstract services and Enum for OHLC',
    packages=find_packages()
)
